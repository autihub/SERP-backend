# ===== 后端环境变量模板 =====
# 复制为 .env（或导出到 shell），按本地实际情况修改。
# 切勿把真实密码提交到仓库。

# 数据库
DB_HOST=localhost
DB_PORT=3306
DB_NAME=serp
DB_USER=root
DB_PASSWORD=changeme

# Spring Profile
SPRING_PROFILES_ACTIVE=dev
